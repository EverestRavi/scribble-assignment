# Quickstart

No new dependencies are introduced in this feature. Standard project setup applies.

1. **Backend**:
   `cd backend && npm install && npm run dev`
2. **Frontend**:
   `cd frontend && npm install && npm run dev`
